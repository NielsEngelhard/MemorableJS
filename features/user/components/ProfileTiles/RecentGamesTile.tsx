import Card from "@/components/ui/card/Card";
import { UserProfileModel } from "../../models"

interface Props {
    userProfile: UserProfileModel;
}

export default function RecentGamesTile({ userProfile }: Props) {
    return (
        <Card>
            WIP
        </Card>
    )
}