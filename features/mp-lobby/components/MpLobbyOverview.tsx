import { MpLobbyModel } from "../models"

interface Props {
    lobby: MpLobbyModel;
}

export default function MpLobbyOverview({ lobby }: Props) {
    return (
        <div>
            lol
        </div>
    )
}